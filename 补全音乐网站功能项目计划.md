# 补全音乐网站功能项目计划

## 一、项目背景

原始项目：

```text
E:\Web\前端小组作业-音乐网站
```

原始仓库：

```text
https://github.com/goodnight-wanan/alg
```

原始项目是一个静态展示型半成品，页面包括：

- 首页
- 歌单分类页
- 歌单详情页
- 登录页
- 注册页

主要问题是数据写死、搜索无功能、歌单筛选无功能、登录注册无状态、播放器只能播放一首固定音乐、页面固定宽度导致移动端不可用。

---

## 二、改造方案

采用方案 B：

- Vue 3
- Vite
- Vue Router 4
- Pinia
- localStorage 模拟后端数据

新项目目录：

```text
E:\Web\new music website
```

原项目保持不变，新项目作为独立 Git 分支管理。

---

## 三、当前进度

- 主要功能已完成并持续推送到原仓库 `vue-rewrite` 分支。
- 最新提交：`7a61c11 feat: compress images, add profile and avatar, adjust auth flow`。
- 原 `master` 分支与原本地目录未改动。
- 约定：每次项目改动完成后自动提交并推送到 GitHub `vue-rewrite` 分支备份。

### 已完成的优化

- 收藏歌曲 / 收藏歌单 / 播放历史按用户 ID 分区存储，登录/退出自动切换数据，未登录禁用收藏。
- 播放历史统一由播放器 store 监听 `currentSong` 记录，覆盖上一首/下一首/队列/自动切歌。
- 播放模式明确为：顺序播放 / 列表循环 / 单曲循环 / 随机播放；顺序播放到末尾停止。
- 搜索关键字、歌曲/歌单标签、分类筛选（类型/心情/年代/地区）同步到 URL，支持刷新和分享。
- 图片压缩：不透明 PNG 转为 JPG，JPG 统一重压缩；封面增加 `object-fit: cover` 全覆盖。
- 注册成功后返回登录页重新登录；登录成功后返回主页 `/`。
- 登录后导航栏显示圆形首字母头像，点击进入新增的「个人中心」`/profile`。

---

## 四、待办事项

### 高优先级

1. **歌曲音频仍共用同一段音频**：`src/data/musicData.js` 中所有歌曲使用 `demoAudio`，实际播放仍是同一首《只因你太美》。建议为每首歌配置独立音频，或至少准备 3–5 首不同音频；当前唯一 MP3 约 9.7MB，也建议压缩或改为短片段。
2. **README 与实际功能脱节**：播放器模式、歌单详情能力、目录结构需要按现状更新。
3. **“收藏歌单”只有数据没有入口**：`toggleFavoritePlaylist` / `isFavoritePlaylist` 没有 UI 调用，`我的音乐` 的「收藏歌单」标签恒为空；建议补收藏按钮，或删除该功能与标签并同步 README。
4. **首页死链**：歌手、新碟、客户端、VIP 等点击无响应，建议实现或隐藏。

### 中优先级

5. **清理死代码与未使用资源**：
   - `user.js` 的 `favoriteSongObjects`；`player.js` 的 `seek(seconds)`。
   - `src/assets/hero.png`、`vue.svg`、`vite.svg`；`public/icons.svg`。
   - 以下 5 个未引用图片，等待确认是否删除：
     - `15a7b849c9bb53eb877bed708b5c447.png`（≈ carousel2）
     - `270fe09c391f9788ccce35fe7027572.png`（≈ carousel4）
     - `47fa988faa79d658e52d0dd08d5810e.png`（≈ carousel3）
     - `7e852783ebb7c3ebbf9473a8488c530.png`（≈ carousel1）
     - `微信图片_20231212204314.jpg`
6. **样式架构**：`src/utils/pageCss.js` 按页动态加载原 CSS，`body` 背景和全局重置随路由切换，易闪烁/冲突；建议统一到组件或根容器类。
7. **导航重复**：首页使用 `HomeView` 自带 header，其它页面使用 `SiteNav`，建议合并为一个组件并处理死链。

### 长期 / 工程化

8. **密码哈希**：`src/utils/storage.js` 的 `hashPassword` 为自定义弱哈希，接入真实后端前应改为服务端校验和安全哈希（bcrypt/PBKDF2）。
9. **登录弹窗与消息安全**：`openAuthWindow` 未处理弹窗被拦截的情况；`App.vue` 接收 `message` 未校验 `event.origin`。
10. **无障碍与语义**：大量 `href="javascript:;"` / `href="#"` 应改为 `button`；歌单详情表单缺 `label`、存在内联样式。
11. **工程化**：增加 ESLint / Prettier。
12. **歌单详情固定尺寸**：`歌单页面.css` 中 `.mybody` 固定宽高与黑色边框存在隐患，用户要求暂不修改。

---

## 五、本地运行方式

```bash
cd "E:\Web\new music website"
npm install
npm run dev
```

构建：

```bash
npm run build
```
