# 补全音乐网站功能项目计划

## 一、项目背景

原始项目：

```text
E:\Web\前端小组作业-音乐网站
```

原始仓库：

```text
https://github.com/goodnight-wanan/alg
```

原始项目是一个静态展示型半成品，页面包括：

- 首页
- 歌单分类页
- 歌单详情页
- 登录页
- 注册页

主要问题是数据写死、搜索无功能、歌单筛选无功能、登录注册无状态、播放器只能播放一首固定音乐、页面固定宽度导致移动端不可用。

---

## 二、改造方案

采用方案 B：

- Vue 3
- Vite
- Vue Router 4
- Pinia
- localStorage 模拟后端数据

新项目目录：

```text
E:\Web\new music website
```

原项目保持不变，新项目作为独立 Git 分支管理。

---

## 三、当前状态

- 页面、播放器、数据、用户、交互、响应式等主要功能已完成。
- 已推送到原仓库的 `vue-rewrite` 分支，最新提交：`e165f15 fix: close auth window and refresh opener state`。
- 原 `master` 分支与原本地目录未改动。

---

## 四、下一步优化建议（按优先级）

### 高优先级（影响正确性）

1. **歌曲音频目前仍共用同一段音频**：`src/data/musicData.js` 中所有歌曲都使用 `demoAudio`，实际播放仍是同一首《只因你太美》，原计划中的“只能播放一首”问题尚未真正解决。建议为每首歌配置独立音频，至少准备 3–5 首不同音频；当前唯一 MP3 约 9.7MB，也应压缩或改为短片段。
2. **收藏与播放历史未按用户隔离**：`src/stores/user.js` 中 `favorite-songs`、`favorite-playlists`、`play-history` 为全局键，不同账号会共享数据。建议按用户 ID 分区存储，并在未登录时禁用收藏或提示登录。
3. **最近播放记录不完整**：`recordPlay` 只在页面点击歌曲时调用，播放器的上一首/下一首/队列点击以及自动切歌均未记录。建议将记录逻辑收敛到播放器 store 的 `currentSong` 变化监听中。

### 中优先级（体验）

4. **播放模式语义与行为不一致**：当前“顺序播放”到末尾会回到第一首继续播，实际等同于“列表循环”。建议明确为顺序 / 列表循环 / 单曲循环 / 随机，或让顺序播放到末尾停止。
5. **搜索与筛选状态未写入 URL**：搜索页输入不同步 `query`，分类页刷新后筛选丢失。建议搜索防抖后同步 URL，分类筛选参数也写入 query，便于刷新和分享。
6. **歌单详情的“添加歌曲”为临时状态**：`src/views/PlaylistView.vue` 中 `localSongs` 未持久化，离开页面即丢失；`URL.createObjectURL` 未释放，`Date.now()` 生成 ID 存在重复风险。建议改为 Pinia + localStorage 持久化的自定义歌单，或先明确标注为演示功能。
7. **首页存在死链**：歌手、新碟、客户端、VIP 等点击无响应，建议实现或隐藏。
8. **登录注册链路缺口**：忘记密码、QQ/微信登录仍为空壳；登录页跳转注册页会丢失 `redirect`，注册后固定跳转 `/mine`。建议将 `redirect` 全程传递。

### 长期 / 工程化

9. **密码哈希需替换**：`src/utils/storage.js` 中的 `hashPassword` 为自定义弱哈希，本地演示可用，接入真实后端前应改为服务端校验并使用安全哈希（如 bcrypt/PBKDF2）。
10. **图片与音频体积优化**：`back.png` 约 4.5MB、`carousel2.png` 约 1.1MB，另有多张 500KB+ 图片，建议压缩或转 WebP。
11. **样式管理**：`src/utils/pageCss.js` 按页动态加载原 CSS，易产生冲突和切换闪烁，建议逐步合并到组件或统一样式。
12. **工程化与无障碍**：增加 ESLint/Prettier；清理未使用资源；将大量 `href="javascript:;"` / `href="#"` 改为 `button` 或 `RouterLink`，并为播放器按钮补充 `aria-label`。

---

## 五、本地运行方式

```bash
cd "E:\Web\new music website"
npm install
npm run dev
```

构建：

```bash
npm run build
```
