# 悦音音乐全栈项目计划（精简上下文版）

> 最后更新：2026 年 8 月 28 日
> 本文件是后续开发的主要上下文。恢复任务时优先阅读本文件和根目录 `README.md`，不再依赖旧对话记录。

## 下一步执行卡（恢复任务后直接执行）

### 当前状态

- 阶段 1「后端基础环境」已经完成并通过验证。
- Docker 中 `api`、`database` 均可健康运行，`media_data` 用于未来持久化音频。
- 用户前台仍使用 Mock 登录和 Mock 音乐数据。

### 下一项任务

执行阶段 2「真实用户与鉴权」，只完成后端闭环，暂不修改用户前台、管理后台和音频上传。

### 建议执行顺序

1. 在 `server/prisma/schema.prisma` 增加 `User`、`RefreshToken`、`UserRole` 和 `UserStatus`。
2. 创建并执行第一份 Prisma migration，再通过 DBX 辅助确认表结构；Prisma migration 仍是结构变更唯一来源。
3. 增加 Auth / Users 模块以及注册、登录 DTO 校验。
4. 使用 Argon2id 或 bcrypt 保存密码哈希。
5. 实现 Access Token、Refresh Token、退出登录和刷新令牌轮换。
6. 实现 `GET /api/auth/me`、JWT 守卫和管理员角色守卫。
7. 增加单元测试和端到端测试，使用 Docker PostgreSQL 完成真实接口验收。
8. 更新本执行卡、README 和阶段状态，向用户汇报并等待继续指令。

### 本阶段验收

- 注册、登录、刷新、退出和当前用户接口全部通过。
- 重复账号、错误密码、无效 Token、过期 Token 和越权访问有明确错误。
- 密码与 Refresh Token 均不以明文写入数据库。
- `npm run lint`、前后端构建、后端测试、Prisma 校验、npm audit 和 Docker 健康检查通过。

### 本阶段禁止扩展

- 不开始开发 `admin/` 管理后台页面。
- 不实现歌曲上传、FFmpeg 或音频 Range 接口。
- 不一次性替换现有前端 `localStorage` 登录。
- 不添加 Redis、Nginx、消息队列或对象存储。

> 维护规则：每完成一个阶段，必须先更新本执行卡中的“当前状态、下一项任务、验收和禁止扩展”，再提交 GitHub。

## 0. 恢复上下文摘要

- 项目目录：`E:\Web\new music website`
- GitHub 仓库：`https://github.com/goodnight-wanan/alg.git`
- 开发分支：`vue-rewrite`
- 项目定位：个人开发、GitHub 开源、小范围分享，不以商业化和大规模用户为目标。
- 用户前台：Vue 3 + Vite + Vue Router + Pinia，主要页面和交互已经基本完成。
- 后端：`server/`，使用 NestJS 11 + Prisma 6.12 + PostgreSQL 17。
- 本地编排：`compose.yaml`，目前包含 `api`、`database` 和持久化音频卷 `media_data`。
- 数据查看：DBX 0.5.97，程序位于 `E:\DBX\dbx.exe`，可连接本地 PostgreSQL。
- 后台管理网站：计划新增 `admin/`，仍使用 Vue 3 + Vite，供管理员维护歌曲、用户和站点数据。
- 下一阶段：用户表、管理员角色、注册、登录、JWT、刷新令牌和当前用户接口。
- 硬性约束：音频文件不写入 PostgreSQL、不提交 GitHub；上传内容必须进入持久化目录；不得回退现有前端体验。

## 1. 产品目标与边界

项目最终由三个应用和两个持久化区域组成：

```text
用户音乐前台（现有 Vue） ─┐
                         ├─> NestJS API ─> PostgreSQL
后台管理网站（计划 Vue） ─┘             └> 音频 / 封面持久化目录
```

### 用户音乐前台

- 浏览首页、歌单、排行榜、新碟和歌手。
- 搜索、播放、收藏歌曲、收藏歌单、创建个人歌单和查看播放历史。
- 修改个人资料、头像和密码。
- 普通用户默认不能进入后台上传歌曲。

### 后台管理网站

- 管理员登录和权限校验。
- 仪表盘：用户数、歌曲数、播放量和存储占用。
- 歌曲管理：上传、编辑、试听、上下架、删除和批量处理。
- 歌手、专辑、分类、歌单和排行榜管理。
- 用户管理：查看、禁用、恢复和调整角色。
- 后台使用独立路由和构建产物，但复用同一个 NestJS API。

### 项目边界

- 不做微服务、消息队列、Kubernetes 和复杂分布式架构。
- 初期不引入 Redis；出现明确缓存需求后再评估。
- 开发阶段不需要 Nginx；部署到个人 VPS 时再用于 HTTPS、静态文件和反向代理。
- GitHub Pages 只能部署静态前端，完整项目仍需要可运行 Node.js 与 PostgreSQL 的服务器或平台。

## 2. 已确定技术方案

### 前台与后台

- 用户前台：Vue 3、Vite、Vue Router、Pinia。
- 管理后台：计划使用 Vue 3、Vite、Vue Router、Pinia，与用户前台保持相同技术栈。
- 请求层：统一封装 `src/api/`，处理基础地址、Token、401、错误提示和请求取消。

### 后端与数据库

- API：NestJS 11，采用单体模块化结构。
- ORM：Prisma 6.12。
- 数据库：PostgreSQL 17。
- 鉴权：JWT Access Token + Refresh Token。
- 密码：服务端使用 Argon2id 或 bcrypt 哈希，浏览器不保存密码和密码哈希。
- 权限：至少包含 `USER` 与 `ADMIN` 两种角色。
- 容器：Docker Compose 统一运行 API 与 PostgreSQL。

## 3. 音频与图片存储方案

### 曲库目标

- 网站初始至少提供 20 首可正常播放的歌曲。
- 只使用自己拥有权利、获得授权或允许公开分发的音频。
- 受版权保护的完整音频不得直接提交到 GitHub 仓库。

### 双来源模型

每首歌曲保存 `sourceType`：

- `LOCAL`：管理员上传到网站服务器的音频。
- `REMOTE`：数据库保存可信网络音频 URL。

统一播放入口：

```text
GET /api/audio/:songId
```

- 本地音频由后端支持 HTTP Range 流式返回，保证拖动进度正常。
- 网络音频只允许 `http` / `https`，并使用可信域名白名单，避免任意内网请求风险。
- 网络地址不可用时返回统一错误，前端提示资源暂时无法播放。

### 上传与压缩

- 后台上传入口只允许管理员或被授权账号使用。
- 输入格式计划支持 MP3、M4A/AAC、OGG、WAV 和 FLAC。
- 服务端使用 FFmpeg 统一转码为浏览器兼容的 MP3，目标码率默认 128 kbps、44.1 kHz、双声道。
- 20 首平均 4 分钟的 128 kbps 音频约占 75 MB，实际目标控制在约 60–100 MB。
- 上传原文件只作为临时文件；转码成功后默认删除原文件，避免重复占用磁盘。
- 单文件默认最大 50 MB，可通过 `MAX_AUDIO_UPLOAD_MB` 调整。
- 封面统一转成 WebP，并限制尺寸和文件大小。

### 持久化位置

- Docker 容器内目录：`/app/uploads`
- Docker 持久化卷：`media_data`
- 本机直接运行后端时：`server/uploads/`
- PostgreSQL 只保存歌曲信息、存储路径、远程 URL、文件大小、格式、时长和码率。
- `uploads/`、数据库数据卷和真实音频不进入 Git。
- 如果未来部署平台不提供永久磁盘，再迁移到 S3 兼容对象存储；现阶段不提前增加这项复杂度。

## 4. 计划中的核心数据模型

- `User`：账号、密码哈希、昵称、头像、角色、状态。
- `RefreshToken`：刷新令牌哈希、过期时间、设备信息。
- `Artist`：歌手资料和头像。
- `Album`：专辑资料、封面和发布时间。
- `Song`：歌曲资料、来源类型、音频路径或 URL、时长、码率、状态。
- `Category`：类型、心情、年代和地区标签。
- `Playlist` / `PlaylistSong`：官方歌单与用户歌单。
- `FavoriteSong` / `FavoritePlaylist`：用户收藏。
- `PlayHistory`：播放历史、次数和最后播放时间。
- `FileAsset`：音频、封面和头像文件元数据。

当前前端歌曲 ID 将保留为 `publicId`，方便迁移现有 Mock 数据和浏览器数据。

## 5. 当前完成状态

### 已完成：用户音乐前台

- 首页、分类歌单、歌单详情、排行榜、新碟、歌手、搜索、登录、注册、我的音乐、个人中心和 404。
- 全局播放器、播放队列、播放模式、音量、进度拖动和 Media Session。
- 收藏歌曲、收藏歌单、自建歌单、播放历史和搜索历史的前端 Mock 流程。
- WebP 头像上传、前端验证码密码修改和退出登录。
- 响应式桌面端、平板和移动端布局。
- 前端 lint 与生产构建通过。

### 已完成：后端第一阶段

- 创建 `server/` NestJS 项目。
- 接入 Prisma 与 PostgreSQL。
- 创建 Dockerfile 和 Docker Compose。
- `GET /api/health` 会实际查询数据库。
- API 与数据库容器健康检查通过。
- 单元测试、端到端测试、lint、构建和 npm 官方安全审计通过。
- DBX 使用 `music_admin` 本地开发参数连接 PostgreSQL，仅用于查看和辅助排查。
- Compose 已预留持久化音频卷 `media_data`。

### 当前仍是 Mock 的功能

- 注册、登录、用户资料和密码。
- 歌曲、歌手、专辑、歌单、排行榜和搜索数据。
- 收藏、用户歌单和播放历史。
- 音频仍主要使用前端演示文件。

## 6. 分阶段实施计划

每完成一个阶段：执行验证、向用户汇报、等待继续指令；除非用户明确要求，不跨阶段开发。

### 阶段 1：后端基础环境（已完成）

- NestJS、Prisma、PostgreSQL、Docker Compose、健康检查。

### 阶段 2：真实用户与鉴权（下一阶段）

- 建立 `User`、`RefreshToken` 和角色模型。
- 实现注册、登录、刷新 Token、退出登录和 `GET /api/auth/me`。
- 密码服务端哈希，Refresh Token 只保存哈希。
- 增加管理员角色和鉴权守卫。
- 暂不改动前端登录页面，先通过接口测试完成后端闭环。

验收：注册、登录、刷新、退出、未授权和管理员权限测试全部通过。

### 阶段 3：曲库与后台管理基础

- 建立歌手、专辑、歌曲、分类和文件模型。
- 新建 `admin/` Vue 管理后台骨架。
- 实现管理员登录、歌曲列表、创建、编辑、上下架和删除。
- 实现音频 / 封面上传、格式校验、FFmpeg 转码和失败清理。
- 实现本地音频 Range 播放和可信远程 URL 播放。

验收：管理员可通过网页上传歌曲，上传后用户接口可播放，重启容器后文件仍存在。

### 阶段 4：初始 20 首曲库与前台数据迁移

- 准备至少 20 首可合法使用、已压缩的音频。
- 编写数据库 Seed，导入歌曲、歌手、专辑、分类和官方歌单。
- 新增前端 `src/api/` 与 `stores/catalog.js`。
- 替换 `src/data/musicData.js` 的业务数据引用。
- 补充加载、空状态、失败重试、分页和搜索参数。

验收：首页、歌单、歌手、排行榜、新碟、搜索和播放器全部使用后端数据。

### 阶段 5：用户业务迁移

- 收藏歌曲、收藏歌单、用户歌单和播放历史迁移到 PostgreSQL。
- 个人中心资料、WebP 头像和密码修改改为真实接口。
- 前端处理 Token 过期、401 和重新登录。

验收：刷新页面、重新登录和不同浏览器访问时数据保持一致。

### 阶段 6：部署与开源整理

- 选择个人服务器或支持持久磁盘的平台。
- 生产环境更换数据库密码、JWT 密钥和 CORS 域名。
- 需要时加入 Nginx、HTTPS、日志轮转和数据库备份。
- 提供 `.env.example`、初始化说明、演示账号和合法示例数据。
- 确定并添加开源许可证；真实受版权保护音频不随仓库发布。

## 7. 主要接口规划

```text
GET    /api/health

POST   /api/auth/register
POST   /api/auth/login
POST   /api/auth/refresh
POST   /api/auth/logout
GET    /api/auth/me

GET    /api/songs
GET    /api/songs/:id
GET    /api/audio/:songId
GET    /api/artists
GET    /api/albums
GET    /api/playlists
GET    /api/playlists/:id
GET    /api/rankings
GET    /api/search

POST   /api/admin/songs
POST   /api/admin/songs/upload
PATCH  /api/admin/songs/:id
DELETE /api/admin/songs/:id

GET    /api/me/favorite-songs
POST   /api/me/favorite-songs/:songId
DELETE /api/me/favorite-songs/:songId
GET    /api/me/playlists
GET    /api/me/history
```

所有列表接口统一支持分页；错误统一返回状态码、错误代码和可展示消息。

## 8. 本地命令与连接信息

前端：

```bash
npm install
npm run dev
```

后端与数据库：

```bash
npm run docker:up
npm run docker:logs
npm run docker:down
```

健康检查：`http://localhost:3000/api/health`

DBX 本地开发连接：

```text
类型：PostgreSQL
主机：localhost
端口：5432
用户名：music_admin
密码：music_dev_password
数据库：music_site
```

以上密码仅用于本地开发，部署时必须通过环境变量替换。

## 9. 开发原则

- 优先完成可验证的闭环，不一次性重写全部前端。
- 每阶段先写数据库结构和接口测试，再接页面。
- 不把音频、密码、Token、生产密钥提交 GitHub。
- 不自动下载来源不明的网络歌曲。
- 不为个人项目过早增加企业级基础设施。
- 保持现有粉色 / 毛玻璃视觉体系和移动端体验。
- 每次提交前至少执行相关 lint、测试、构建和 `git diff --check`。
