# 补全音乐网站功能项目计划

## 一、项目背景

原始项目：

```text
E:\Web\前端小组作业-音乐网站
```

原始仓库：

```text
https://github.com/goodnight-wanan/alg
```

原始项目是一个静态展示型半成品，页面包括：

- 首页
- 歌单分类页
- 歌单详情页
- 登录页
- 注册页

主要问题是数据写死、搜索无功能、歌单筛选无功能、登录注册无状态、播放器只能播放一首固定音乐、页面固定宽度导致移动端不可用。

---

## 二、改造方案

采用方案 B：

- Vue 3
- Vite
- Vue Router 4
- Pinia
- localStorage 模拟后端数据

新项目目录：

```text
E:\Web\new music website
```

原项目保持不变，新项目作为独立 Git 分支管理。

---

## 三、当前进度

- 盘点日期：2026 年 8 月 28 日。
- 前端主要功能与本轮恢复验收均已完成，目标分支为原仓库 `vue-rewrite`，相关改动提交并推送至该分支。
- 本轮开始代码基线：`20c1f19 fix: make rank charts centering robust across browsers`。
- 最新功能修复提交：`d83d90d fix: unify custom playlist cards and detail view`，已同步至 `origin/vue-rewrite`。
- 已完成真实登录弹窗、自建歌单完整流程和桌面端 / 平板 / 移动端浏览器回归；最终 `npm run lint`、`npm run build` 与 `git diff --check` 均通过。
- 原 `master` 分支与原本地目录未改动。
- 约定：每次项目改动完成后自动提交并推送到 GitHub `vue-rewrite` 分支备份。

### 当前网站内容概览

- 路由共 12 个：首页、分类歌单、排行榜、新碟、歌手、歌单详情、搜索、登录、注册、我的音乐、个人中心、404。
- Mock 数据共 106 首歌曲、28 个歌单、72 位聚合歌手，其中 50 首标记为新歌。
- 分类维度包括 10 种类型、8 种心情、6 个年代和 5 个地区（内地、港台、欧美、韩国、日本）。
- 登录、收藏歌曲、收藏歌单、最近播放、搜索历史均使用 `localStorage` 持久化；用户收藏与播放历史按用户 ID 隔离。
- 除登录和注册页外，全站显示固定顶部导航、底部播放器、全局提示和返回顶部按钮。

### 核心架构

- Vue 3 + Vite + Vue Router 4 + Pinia，localStorage 模拟后端。
- 使用 Hash 路由、页面标题更新、登录鉴权守卫和 404 兜底路由。
- 全站统一固定顶部导航 `AppHeader.vue`（`position: fixed` + ResizeObserver 测高），移动端汉堡菜单。
- 新增 `Icon.vue` 全局 SVG 图标组件，全站 emoji / Unicode 字形已替换为 SVG，跨浏览器渲染一致。
- 新增全局提示工具 `src/utils/notice.js`，支持 info / success / error 三种 toast 状态与对应 SVG 图标。
- 通用组件：`AppHeader`、`PlayerBar`、`UserCard`、`Icon`（`SiteNav` 已删除）。
- 首页、登录、注册旧样式已迁入 `src/styles/` 并改为组件静态导入，由 Vite 统一打包；运行时 CSS 注入工具 `pageCss.js` 已删除。

### 已完成的优化（按模块）

- 首页：轮播图支持自动播放、前后切换、圆点跳转、手动暂停和页面隐藏时暂停；推荐歌单、新歌首发、排行榜均支持分类/地区切换和播放联动；响应式容器防溢出，封面按比例缩放，排行榜在不同浏览器下稳定居中。
- 导航：品牌 Logo、SVG 搜索、统一激活态（粉字 + 底部短横线）、登录 CTA、毛玻璃背景、移动端菜单、语义化（aria）；`/` 快捷键可聚焦搜索，移动端会先展开菜单再聚焦输入框。
- 歌单分类页：重写为 functional 体系 + chip 筛选 + 卡片网格 + 空状态；支持名称/描述搜索和类型、心情、年代、地区四维筛选，筛选条件同步 URL。
- 歌单详情页：重写为展示歌单信息 + 歌曲列表（去掉上传表单），支持播放全部、收藏歌单。
- 排行榜页：新增 `/rank`，榜单卡片 + 前 20 名列表（前三金银铜），榜单切换同步 URL，并与导航、首页联动。
- 我的音乐页：`UserCard` 使用默认图片头像并保留统计信息，已移除退出登录按钮；新增“我的歌单”Tab，支持创建、命名、播放和删除歌单，封面自动使用最近加入歌曲图片；自建歌单卡片与收藏歌单样式一致，点击后复用歌单详情页并支持移除歌曲。
- 播放器：支持播放/暂停、上一首/下一首、进度跳转、音量/静音、播放队列、顺序/列表循环/单曲循环/随机播放、歌曲收藏；`playAll` 再次点击可暂停当前列表。
- 用户系统：收藏歌曲/歌单/播放历史/自建歌单按用户 ID 分区；登录与注册改为独立新窗口，登录成功通知并返回原网站，注册成功仅返回登录页，不再自动登录。
- 新碟页：新增 `/album`，品牌渐变 banner + 新碟卡片网格（NEW 角标、悬浮播放），数据取 `isNew` 新歌。
- 歌手页：新增 `/artist`，从歌曲聚合歌手（支持多歌手拆分），搜索 + 地区筛选 + 圆形头像卡片 + 页内歌手详情。
- 搜索页：重构为「热门搜索 / 历史搜索」引导态 + 结果态；歌曲 / 歌单 / 歌手三分类、关键词高亮、相关性排序、整行播放、清除按钮；歌单结果可进入详情，歌手结果可跳转并定位到歌手页。
- 登录与提示：新增 `src/utils/authWindow.js` 管理登录/注册窗口、redirect 与 `postMessage` 通知；`alert` 统一替换为 `notice` toast。
- 可访问性：轮播圆点键盘可达、卡片标题键盘操作、`role` / `aria` 语义化、`prefers-reduced-motion` 降级动画、focus 样式、播放器滑块状态播报。
- 清理与性能：删除 `favoriteSongObjects`、旧 `seek(seconds)`、`openAuthWindow.js`、`pageCss.js`、未用图片 / 图标与 `歌单页面.css`；图片懒加载并统一 WebP，页面 CSS 改为静态依赖，减少运行时加载抖动。

### 近期完成（第二轮 UI / 工程化优化）

- 设计系统：建立 CSS 变量 token（品牌色 / 文字色 / 字体 / surface），全站无衬线字体；`font-weight: 1000` 统一为 `900`；次要文字对比度提升。
- 交互：全站按钮 `cursor: pointer`、统一去除按钮默认边框；「播放全部」按钮固定最小宽度防变形；播放器收藏按钮固定位置、播放 / 暂停图标同步。
- 首页：三个区块 tab 支持方向键与 roving tabindex，卡片可键盘操作；分页点按实际数据动态生成、新歌区最多 4 点、内容不足时不拉伸卡片；排行榜卡片改渐变 + 白色半透明未选中态；「更多」保持一行、歌单文案自适应截断。
- 数据：歌曲扩充到 106 首、歌单 28 个；「日韩」拆分为「韩国」「日本」；地区 tab 只筛 `isNew`。
- 表单与提示：登录 / 注册补可见 label 与 autocomplete；错误 / 提示统一胶囊样式并区分 alert / info 图标；`/` 搜索快捷键全局化。
- 图片：全部 JPG 与照片类 PNG 转为 WebP（质量 80），图片总量约 3.1MB → 1.9MB；删除未引用大图；新增 `convert-images.mjs`。
- 工程化：引入 ESLint + Prettier（`npm run lint` / `npm run format`），并已格式化 src。
- 播放器无障碍：进度 / 音量滑块补 `aria-valuetext`，播放器标题 `aria-live`，队列项键盘可达。

### 近期完成（第三轮交互 / 稳定性优化）

- 播放器快捷键：非表单区域按空格播放/暂停，左右方向键前进/后退 5 秒；鼠标悬停播放器时，上下方向键按 5% 调节音量。
- 系统媒体控制：接入 Media Session API，支持系统级播放、暂停、上一首、下一首及前后跳转 10 秒，并同步播放状态与歌曲元数据。
- 播放稳定性：增加缓冲中加载状态；音频加载失败时停止播放并显示 error toast。
- 队列交互：播放队列支持关闭按钮、点击外部关闭，队列歌曲支持鼠标与键盘播放。
- 搜索联动：搜索结果中的歌手卡片可直接跳转到 `/artist?q=歌手名`；移动端 `/` 快捷键会展开导航搜索框。
- 视觉修正：全站背景图片固定，减少交互重渲染时的背景滚动与卡顿；首页排行榜容器与卡片标题完成跨浏览器居中修复。
- 样式加载：`style.css`、`login.css`、`register.css` 从公共静态目录迁入 `src/styles/`，由对应页面直接导入，不再动态创建 `<link>`。
### 本轮优化与验收（2026 年 8 月 28 日，已完成）

#### 已实现

1. 登录与注册使用独立浏览器窗口；登录成功通过 `postMessage` 同步原页面并返回网站，注册成功返回登录页且不会自动登录。
2. 全站背景图片使用固定背景，避免按钮交互或页面局部重渲染时背景重复滚动造成卡顿。
3. 顶部搜索框聚焦后显示搜索面板；空关键词显示最近搜索，输入关键词后联想匹配歌曲、歌单和歌手。
4. 登录用户头像改为图片展示，新增默认演示头像 `public/assets/imgs/default-avatar.svg`，后端接入后替换为用户头像 URL。
5. “我的音乐”移除退出登录按钮并新增“我的歌单”Tab；支持自定义命名、删除歌单、移除歌曲，歌单封面使用最近加入歌曲封面。
6. 全站歌曲条目和底部播放器均新增“添加到”按钮；可选择已有自建歌单，也可在没有歌单时创建并立即添加。
7. 新增通用组件 `src/components/AddToPlaylistButton.vue`，新增 `src/utils/searchHistory.js`，并扩展 `src/stores/user.js` 的用户级自建歌单持久化。
8. 已配置通用现代 Web 工程技能 `modern-web-engineering`，覆盖 Vue/Nuxt、React/Next、TypeScript、SSR、状态管理、测试、性能与迁移，不按单一框架重复堆叠技能。

#### 已验证

- 登录按钮会打开独立窗口；注册成功会返回登录页并显示成功提示。
- 登录后顶部和“我的音乐”用户卡片均显示图片头像。
- 搜索关键词“周”可显示歌曲、歌单和歌手联想结果。
- “我的音乐”不再显示退出登录按钮，并正常显示“我的歌单”Tab。
- 已实测创建“通勤歌单”、从首页歌曲“添加到”该歌单，最近加入歌曲封面可正确成为歌单封面。
- 已实测“创建并添加”快捷流程、从歌单移除歌曲、删除歌单及对应空状态。
- 已在真实 `window.opener` 场景验证登录成功关闭独立窗口、同步原页面会话并返回原路由。
- 已完成 375px 移动端首页、歌曲列表、搜索联想、歌单弹窗、播放队列和底部播放器回归，无横向溢出。
- 已完成 768px 平板和 1440px 桌面关键布局回归；900px 以下统一使用汉堡导航，`/` 快捷键会先展开菜单再聚焦搜索框。
- 首页排行榜和播放队列的播放区域已改为独立按钮，与“添加到”按钮保持并列，DOM 检查无嵌套交互控件。
- 自建歌单悬浮播放键已与收藏歌单统一定位；点击卡片进入 `/playlist/:id` 详情页，不再在“我的音乐”页展开独立详情区域。
- 最终 `npm run lint`、`npm run build`、`git diff --check` 均已通过，浏览器控制台无错误或警告。

#### 恢复任务完成情况

1. 已验证从歌单移除歌曲、删除歌单，以及“创建并添加”快捷流程。
2. 已完成移动端首页、歌曲列表、搜索面板、歌单弹窗和底部播放器布局回归。
3. 已修复首页排行榜和播放队列中的嵌套交互控件问题。
4. 已验证真实 `window.opener` 登录窗口关闭与原页面同步行为。
5. 已更新 `README.md`，并完成最终 lint、build、diff check。
6. 已修复自建歌单卡片悬浮播放键位置，并将卡片点击行为统一为进入标准歌单详情页。
7. 本轮改动已提交并推送 GitHub `vue-rewrite` 分支备份，最新功能提交为 `d83d90d`。

---

## 四、待办事项（剩余）

### P0：接入后端（详见「六、后端接入计划」）

1. 歌曲 / 歌单 / 歌手 / 搜索 / 排行榜数据改为后端接口。
2. 用户系统改为 JWT 鉴权，删除 `storage.js` 的 `hashPassword`，密码由服务端 bcrypt / PBKDF2 处理。
3. 音频改为后端真实音频 URL（支持 HTTP Range 流式播放）。
4. 收藏 / 播放历史迁移到后端，按用户持久化。
5. 封面改为后端 / 对象存储 URL，降低前端封面复用。

### P1：质量与产品补全

6. 为用户、播放器、筛选、搜索和路由守卫补充单元测试；为注册 → 登录 → 搜索 → 播放 → 收藏链路补充端到端测试。
7. 补充加载态、接口空态、网络错误重试、401 失效登录和离线降级策略。
8. 对桌面端、平板和移动端进行完整浏览器回归，重点检查固定导航、播放器、长标题和小屏队列。
9. 增加歌曲、歌手、歌单的稳定 ID 与分页参数，避免前端仅依赖内存数组和名称定位。

### P2：当前演示限制（非阻塞）

10. 106 首歌曲目前仍共用同一首 demo 音频，接入后端时替换为真实授权音频。
11. 顶部「客户端」「VIP」和首页页脚入口当前只显示演示提示；登录页「忘记密码」「用户协议」、QQ / 微信登录尚未实现真实业务。
12. 韩国 / 日本地区已拆分，但部分歌曲封面仍重复，接入真实曲库或对象存储后替换。
13. 个人中心当前仅展示用户卡片与统计，后续可增加头像、昵称、密码等资料编辑能力。

> 已完成：ESLint / Prettier 已引入；死代码与未用资源已清理；图片已统一 WebP；旧页面 CSS 已改为 Vite 静态导入；播放器交互与错误提示已补强。

---

## 五、本地运行方式

```bash
cd "E:\Web\new music website"
npm install
npm run dev
```

构建：

```bash
npm run build
```

代码检查与格式化：

```bash
npm run lint
npm run format
```

---

## 六、后端接入计划

### 技术栈（建议）

- 后端：Node.js + Express（或 NestJS）
- 数据库：MySQL / PostgreSQL（演示可先用 SQLite）
- 鉴权：JWT（access token）
- 文件：音频与封面先本地目录跑通，再迁移到 OSS / S3 / COS
- 前端：Vite `server.proxy` 代理 `/api`，`.env` 配置 `VITE_API_BASE`

### 主要接口清单（MVP）

```text
POST   /api/auth/register
POST   /api/auth/login
GET    /api/auth/me
GET    /api/songs
GET    /api/songs/:id
GET    /api/playlists
GET    /api/playlists/:id
GET    /api/albums
GET    /api/artists
GET    /api/artists/:id
GET    /api/rank
GET    /api/search?q=&type=
GET    /api/favorites/songs
POST   /api/favorites/songs/:id
DELETE /api/favorites/songs/:id
GET    /api/favorites/playlists
POST   /api/favorites/playlists/:id
DELETE /api/favorites/playlists/:id
GET    /api/history
POST   /api/history
DELETE /api/history
GET    /api/audio/:id   （支持 HTTP Range，流式播放）
```

### 前端改造步骤

1. 新建 `src/api/http.js`（baseURL、Authorization、401 处理、统一错误）。
2. 新建 `src/api/music.js`、`src/api/auth.js` 封装接口。
3. 新建 `src/stores/catalog.js` 统一管理歌曲 / 歌单 / 歌手数据，替换 `data/musicData.js` 的直接引用。
4. `src/stores/user.js` 改为异步登录 / 注册，存 JWT；删除 `hashPassword`。
5. `src/stores/player.js` 播放后端返回的 `song.audio` URL。
6. 分类、搜索、排行榜增加查询参数、分页、加载态、空态和错误重试。
7. 收藏 / 历史改为登录后走接口；游客仅保留当前播放队列，不持久化用户数据。
8. `vite.config.js` 增加 `/api` 代理，新增 `.env` 环境变量。
9. 保留 Media Session、键盘快捷键和播放器队列交互，接口迁移时不得回退现有体验。

### 建议落地顺序

1. 第一阶段：注册 / 登录（JWT）→ 当前用户 → 路由鉴权。
2. 第二阶段：歌曲列表 → 歌单 / 歌手 / 新碟 / 排行榜 → 搜索与分页。
3. 第三阶段：音频 Range 流式播放 → 播放历史 → 收藏歌曲 / 歌单。
4. 第四阶段：对象存储、错误监控、自动化测试、生产环境部署。

---

## 七、阶段验收标准

### 前端现状验收

- `npm run lint` 无错误。
- `npm run build` 成功生成 `dist/`。
- 12 个路由均可访问，受保护页面在未登录时跳转登录页。
- 搜索、筛选、播放、收藏、历史记录和刷新后状态恢复可正常工作。
- 桌面端与移动端导航、播放器、队列和长列表无明显遮挡或横向溢出。

### 后端接入验收

- 前端不再直接依赖 `src/data/musicData.js` 作为业务数据源。
- 密码不在浏览器端存储或处理，JWT 过期后可正确退出并引导重新登录。
- 音频接口支持 Range 请求，拖动进度、上一首/下一首和播放模式保持正常。
- 收藏与播放历史在刷新、重新登录和不同设备间保持一致。
- 主要接口具备统一错误结构、分页参数和基本自动化测试。
