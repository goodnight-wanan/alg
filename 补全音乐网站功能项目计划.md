# 悦音音乐全栈项目计划（精简上下文版）

> 最后更新：2026 年 8 月 30 日
> 本文件是后续开发的主要上下文。恢复任务时优先阅读本文件和根目录 `README.md`，不再依赖旧对话记录。

## 下一步执行卡（恢复任务后直接执行）

### 当前状态

- 阶段 1 至阶段 5 全部完成；阶段 6「部署」已上线运行。
- 前台 http://47.97.166.11 与后台 http://47.97.166.11:8080 均已可访问，曲库、播放、管理全通。
- 服务器已配 Nginx 反向代理、数据库自动备份（cron 每天 3 点，保留 7 天）。
- 已购域名 `goodnight.fashion`，服务器续费至满足备案要求（≥ 3 个月），域名实名审核进行中。

### 下一项任务

完成域名备案并配置 HTTPS。

### 建议执行顺序

1. 等域名实名审核通过。
2. 阿里云 ICP 备案：申请备案服务号 → 提交主体与网站信息 → 初审 → 短信核验 → 管局审核（约 1-2 周）。
3. 备案通过后，把 `goodnight.fashion` 的 A 记录解析到 `47.97.166.11`。
4. 申请 SSL 证书（阿里云免费证书或 Let's Encrypt），下载 Nginx 版证书。
5. Nginx 配置 443 端口启用 HTTPS，并把 80 自动跳转到 443。
6. 更新 API 的 `CORS_ORIGIN`，加入 `https://goodnight.fashion`。
7. 防火墙放行 443 端口。

### 本阶段验收

- https://goodnight.fashion 能正常访问前台与后台，HTTP 自动跳转到 HTTPS。
- HTTPS 下登录、播放、跨域全部正常。

### 本阶段禁止扩展

- 不引入 CDN、WAF 等额外安全产品，先用 Nginx + 证书的最简方案。
- 不改变已完成的部署结构，两个 worktree 与分支继续独立维护。

> 维护规则：每完成一个阶段，必须先更新本执行卡中的“当前状态、下一项任务、验收和禁止扩展”，再提交 GitHub。

## 0. 恢复上下文摘要

- 项目目录：`E:\Web\悦音音乐\new music website`
- GitHub 仓库：`https://github.com/goodnight-wanan/alg.git`
- 开发分支：`vue-rewrite`
- 项目定位：个人开发、GitHub 开源、小范围分享，不以商业化和大规模用户为目标。
- 用户前台：Vue 3 + Vite + Vue Router + Pinia，主要页面和交互已经基本完成。
- 后端：`server/`，使用 NestJS 11 + Prisma 6.12 + PostgreSQL 17。
- 本地编排：`compose.yaml`，目前包含 `api`、`database` 和持久化音频卷 `media_data`。
- 数据查看：DBX 0.5.97，程序位于 `E:\DBX\dbx.exe`，可连接本地 PostgreSQL；DBX 只用于查看和辅助排查，数据库结构变更统一使用 Prisma migration。
- 后台管理网站：同仓库 `admin-dashboard` 分支，worktree 位于 `E:\Web\悦音音乐\admin-dashboard`，使用 Vue 3 + Vite。
- 下一阶段：域名备案 + 配 HTTPS。
- 硬性约束：音频文件不写入 PostgreSQL、不提交 GitHub；上传内容必须进入持久化目录；不得回退现有前端体验。

## 1. 产品目标与边界

项目最终由三个应用和两个持久化区域组成：

```text
用户音乐前台（现有 Vue） ─┐
                         ├─> NestJS API ─> PostgreSQL
后台管理网站（Vue） ─────┘             └> 音频 / 封面持久化目录
```

### 用户音乐前台

- 浏览首页、歌单、排行榜、新碟和歌手。
- 搜索、播放、收藏歌曲、收藏歌单、创建个人歌单和查看播放历史。
- 修改个人资料、头像和密码。
- 普通用户默认不能进入后台上传歌曲。

### 后台管理网站

- 管理员登录和权限校验。
- 数据汇总：歌曲数、已上架数、歌手、专辑、分类和总播放量。
- 歌曲管理：上传、编辑、试听、上下架、删除和批量处理。
- 歌手、专辑、分类、歌单和排行榜管理。
- 用户管理：查看、禁用、恢复和调整角色。
- 后台使用独立路由和构建产物，但复用同一个 NestJS API。

### 项目边界

- 不做微服务、消息队列、Kubernetes 和复杂分布式架构。
- 初期不引入 Redis；出现明确缓存需求后再评估。
- 开发阶段不需要 Nginx；部署到个人 VPS 时再用于 HTTPS、静态文件和反向代理。
- GitHub Pages 只能部署静态前端，完整项目仍需要可运行 Node.js 与 PostgreSQL 的服务器或平台。

## 2. 已确定技术方案

### 前台与后台

- 用户前台：Vue 3、Vite、Vue Router、Pinia。
- 管理后台：计划使用 Vue 3、Vite、Vue Router、Pinia，与用户前台保持相同技术栈。
- 请求层：统一封装 `src/api/`，处理基础地址、Token、401、错误提示和请求取消。

### 后端与数据库

- API：NestJS 11，采用单体模块化结构。
- ORM：Prisma 6.12。
- 数据库：PostgreSQL 17。
- 鉴权：JWT Access Token + Refresh Token。
- 密码：服务端使用 Argon2id 或 bcrypt 哈希，浏览器不保存密码和密码哈希。
- 权限：至少包含 `USER` 与 `ADMIN` 两种角色。
- 容器：Docker Compose 统一运行 API 与 PostgreSQL。

## 3. 音频与图片存储方案

### 曲库目标

- 网站初始至少提供 20 首可正常播放的歌曲。
- 只使用自己拥有权利、获得授权或允许公开分发的音频。
- 受版权保护的完整音频不得直接提交到 GitHub 仓库。

### 双来源模型

每首歌曲保存 `sourceType`：

- `LOCAL`：管理员上传到网站服务器的音频。
- `REMOTE`：数据库保存可信网络音频 URL。

统一播放入口：

```text
GET /api/audio/:songId
```

- 本地音频由后端支持 HTTP Range 流式返回，保证拖动进度正常。
- 网络音频只允许 `http` / `https`，并使用可信域名白名单，避免任意内网请求风险。
- 网络地址不可用时返回统一错误，前端提示资源暂时无法播放。

### 上传与压缩

- 后台上传入口只允许管理员或被授权账号使用。
- 输入格式计划支持 MP3、M4A/AAC、OGG、WAV 和 FLAC。
- 服务端使用 FFmpeg 统一转码为浏览器兼容的 MP3，目标码率默认 128 kbps、44.1 kHz、双声道。
- 20 首平均 4 分钟的 128 kbps 音频约占 75 MB，实际目标控制在约 60–100 MB。
- 上传原文件只作为临时文件；转码成功后默认删除原文件，避免重复占用磁盘。
- 单文件默认最大 50 MB，可通过 `MAX_AUDIO_UPLOAD_MB` 调整。
- 封面统一转成 WebP，并限制尺寸和文件大小。

### 持久化位置

- Docker 容器内目录：`/app/uploads`
- Docker 持久化卷：`media_data`
- 本机直接运行后端时：`server/uploads/`
- PostgreSQL 只保存歌曲信息、存储路径、远程 URL、文件大小、格式、时长和码率。
- `uploads/`、数据库数据卷和真实音频不进入 Git。
- 如果未来部署平台不提供永久磁盘，再迁移到 S3 兼容对象存储；现阶段不提前增加这项复杂度。

## 4. 计划中的核心数据模型

- `User`：账号、密码哈希、昵称、头像、角色、状态。
- `RefreshToken`：刷新令牌哈希、过期时间、设备信息。
- `Artist`：歌手资料、地区和头像。
- `Album`：专辑资料、封面和发布时间。
- `Song`：歌曲资料、来源类型、音频路径或 URL、时长、码率、状态。
- `Category`：带分组类型（类型/心情/年代/地区/榜单/特色）的标签。
- `Playlist` / `PlaylistSong`：官方歌单与用户歌单。
- `FavoriteSong` / `FavoritePlaylist`：用户收藏。
- `PlayHistory`：播放历史、次数和最后播放时间。
- `FileAsset`：音频、封面和头像文件元数据。

当前前端歌曲 ID 将保留为 `publicId`，方便迁移现有 Mock 数据和浏览器数据。

## 5. 当前完成状态

### 已完成：用户音乐前台

- 首页、分类歌单、歌单详情、排行榜、新碟、歌手、搜索、登录、注册、我的音乐、个人中心和 404。
- 全局播放器、播放队列、播放模式、音量、进度拖动和 Media Session。
- 收藏歌曲、收藏歌单、自建歌单、播放历史和搜索历史的前端 Mock 流程。
- WebP 头像上传、前端验证码密码修改和退出登录。
- 响应式桌面端、平板和移动端布局。
- 前端 lint 与生产构建通过。

### 已完成：后端基础与鉴权

- 创建 `server/` NestJS 项目。
- 接入 Prisma 与 PostgreSQL。
- 创建 Dockerfile 和 Docker Compose。
- `GET /api/health` 会实际查询数据库。
- API 与数据库容器健康检查通过。
- 单元测试、端到端测试、lint、构建和 npm 官方安全审计通过。
- DBX 使用下方本地开发参数连接 PostgreSQL；数据库结构以 Prisma schema 和 migration 为准。
- Compose 已预留持久化音频卷 `media_data`。

### 已完成：曲库与后台管理基础

- 新增 `Artist`、`Album`、`Category`、`Song`、`SongCategory` 和 `FileAsset` 模型及第二份 migration。
- 新增公开曲库、音频、图片接口，以及管理员歌曲 CRUD、批量状态、批量删除和管理员试听接口。
- 本地音频支持 multipart 上传、FFmpeg 转 MP3、HTTP Range；封面统一转 WebP。
- 远程音频使用域名白名单，默认未配置时拒绝全部远程地址。
- 管理后台在独立 `admin-dashboard` 分支和同级 worktree 中完成登录、录入、上传、搜索、筛选、编辑、试听、上下架、删除与批量操作。
- 歌手支持地区与头像维护；分类支持按分组的增删改查，别名自动加分组前缀；专辑支持封面上传与替换。
- 管理后台曲库页按「歌手/分类/专辑」「上传/录入歌曲」「歌曲列表」三个顶层 tab 分区，歌曲标签改为分组可勾选芯片，并新增数据汇总与弹窗提示。
- 新增 `GET /api/admin/stats` 以及歌手/分类/专辑的查改删接口；删除歌手/专辑前校验引用并清理头像/封面文件。
- 修复弹窗注册/登录后主窗口登录态不即时刷新的问题。
- 真实上传验证覆盖 MP3/WebP 生成、Range 206、容器重启持久化和删除后的数据库及物理文件清理。
- 前台、后台、后端 lint、构建、单元测试、PostgreSQL E2E 和三套生产依赖安全审计通过。

### 当前仍是演示或本地功能

- QQ、微信登录、忘记密码、客户端和 VIP 入口仍是演示功能。
- 搜索历史和密码修改页面验证码仅保存在浏览器本地。

## 6. 分阶段实施计划

每完成一个阶段：执行验证、向用户汇报、等待继续指令；除非用户明确要求，不跨阶段开发。

### 阶段 1：后端基础环境（已完成）

- NestJS、Prisma、PostgreSQL、Docker Compose、健康检查。

### 阶段 2：真实用户与鉴权（已完成）

- 建立 `User`、`RefreshToken` 和角色模型。
- 实现注册、登录、刷新 Token、退出登录和 `GET /api/auth/me`。
- 密码服务端哈希，Refresh Token 只保存哈希。
- 增加管理员角色和鉴权守卫。
- 暂不改动前端登录页面，先通过接口测试完成后端闭环。

验收：注册、登录、刷新、退出、未授权和管理员权限测试全部通过。

已完成内容：

- 新增 `User`、`RefreshToken`、`UserRole` 和 `UserStatus` Prisma 模型及首份 migration。
- 注册支持用户名与邮箱唯一校验，登录支持用户名或邮箱。
- 密码使用 bcrypt 哈希；Refresh Token 仅保存 SHA-256 摘要。
- Access Token 与 Refresh Token 使用独立密钥和有效期，刷新时执行令牌轮换。
- 新增 JWT 守卫、管理员角色守卫和 `GET /api/auth/me`。
- Docker Compose 增加一次性 `migrate` 服务，在 API 启动前应用 migration。
- 单元测试和真实 PostgreSQL 端到端测试已覆盖重复账号、错误密码、无效/过期 Token、禁用账号和越权访问。

### 阶段 3：曲库与后台管理基础（已完成）

- 建立歌手、专辑、歌曲、分类和文件模型。
- 在同仓库独立 `admin-dashboard` 分支和 `E:\Web\悦音音乐\admin-dashboard` worktree 中建立 Vue 管理后台。
- 实现管理员登录、歌曲列表、创建、编辑、上下架和删除。
- 实现音频 / 封面上传、格式校验、FFmpeg 转码和失败清理。
- 实现本地音频 Range 播放和可信远程 URL 播放。

验收：管理员可通过网页上传歌曲，上传后用户接口可播放，重启容器后文件仍存在。

### 阶段 4：初始 20 首曲库与前台数据迁移（已完成）

- 准备至少 20 首可合法使用、已压缩的音频。
- 编写数据库 Seed，导入歌曲、歌手、专辑、分类和官方歌单。
- 新增前端 `src/api/` 与 `stores/catalog.js`。
- 替换 `src/data/musicData.js` 的业务数据引用。
- 补充加载、空状态、失败重试、分页和搜索参数。

验收：首页、歌单、歌手、排行榜、新碟、搜索和播放器全部使用后端数据。

已完成内容：

- Seed 在本地生成 20 首原创合成演示 MP3、专辑 / 歌单 WebP 封面，并导入 5 位歌手、10 张专辑、19 个分类和 12 个官方歌单。
- 用户前台新增统一曲库 API 请求层、Pinia 曲库状态与数据规范化层，删除旧 `src/data/musicData.js` 业务依赖。
- 首页、歌单、歌手、新碟、排行榜、搜索和播放器使用真实 API；新碟页按后端专辑唯一展示并播放专辑完整曲目。
- 加载、空曲库、失败重试、分页、搜索和音频加载失败状态已覆盖。
- 前端、管理后台和后端 lint / 构建、后端单元测试与 PostgreSQL E2E、Prisma 校验、20 首 Seed、HTTP Range 206 和 Edge 桌面 / 移动端验收通过。

### 阶段 5：用户业务迁移（已完成）

- 收藏歌曲、收藏歌单、用户歌单和播放历史迁移到 PostgreSQL。
- 个人中心资料、WebP 头像和密码修改改为真实接口。
- 前端处理 Token 过期、401 和重新登录。

验收：刷新页面、重新登录和不同浏览器访问时数据保持一致。

已完成内容：

- 新增歌曲收藏、歌单收藏、用户歌单、播放历史、用户头像资产关系及第四份 Prisma migration。
- 新增 `/api/me` 资料、头像、改密、收藏、用户歌单和播放历史接口，支持分页、幂等收藏、重复歌曲检查和所有权校验。
- 前台新增统一鉴权请求层，完成真实注册、登录、退出、当前用户、401 自动刷新和刷新失败清理。
- 收藏、用户歌单与播放历史使用乐观更新，并在请求失败时回滚；个人资料和 WebP 头像持久化到后端。
- 改密后撤销该用户全部 Refresh Token，并引导重新登录。
- 前后台 lint / 构建、后端单元测试、真实 PostgreSQL E2E、Docker 健康检查和 Edge 浏览器流程通过。

### 阶段 6：部署与开源整理（基本完成，剩备案 + HTTPS）

- ✅ 已上线：阿里云轻量服务器（杭州，2 核 4G，Ubuntu），公网 IP 47.97.166.11。
- ✅ 已部署：前台 /var/www/html、后台 /var/www/admin（8080 端口）、API + PostgreSQL 用 Docker Compose 运行，Nginx 反向代理 /api/。
- ✅ 已配置：生产数据库密码、JWT 密钥、CORS、数据库自动备份（cron 每天 3 点，保留 7 天）。
- ✅ 已开源：MIT 许可证，真实受版权音频不随仓库发布。
- ⏳ 剩余：域名备案 + 配 HTTPS（步骤见顶部执行卡）。

## 7. 主要接口规划

```text
GET    /api/health

POST   /api/auth/register
POST   /api/auth/login
POST   /api/auth/refresh
POST   /api/auth/logout
GET    /api/auth/me

GET    /api/songs
GET    /api/songs/:id
GET    /api/audio/:songId
GET    /api/artists
GET    /api/albums
GET    /api/playlists
GET    /api/playlists/:id
GET    /api/rankings
GET    /api/search

GET    /api/admin/stats
POST   /api/admin/songs
POST   /api/admin/songs/upload
PATCH  /api/admin/songs/:id
DELETE /api/admin/songs/:id
POST   /api/admin/artists
PATCH  /api/admin/artists/:id
DELETE /api/admin/artists/:id
POST   /api/admin/categories
PATCH  /api/admin/categories/:id
DELETE /api/admin/categories/:id
POST   /api/admin/albums
PATCH  /api/admin/albums/:id
DELETE /api/admin/albums/:id

GET    /api/me/favorite-songs
POST   /api/me/favorite-songs/:songId
DELETE /api/me/favorite-songs/:songId
GET    /api/me/playlists
GET    /api/me/history
```

所有列表接口统一支持分页；错误统一返回状态码、错误代码和可展示消息。

## 8. 本地命令与连接信息

前端：

```bash
npm install
npm run dev
```

后端与数据库：

```bash
npm run docker:up
npm run docker:logs
npm run docker:down
```

健康检查：`http://localhost:3000/api/health`

DBX 本地开发连接：

```text
类型：PostgreSQL
主机：localhost
端口：5432
用户名：music_admin
密码：music_dev_password
数据库：music_site
```

以上密码仅用于本地开发，部署时必须通过环境变量替换。
DBX 位于 `E:\DBX\dbx.exe`，仅用于查看数据和辅助排查；建表、改表和索引变更必须提交 Prisma migration。

## 9. 开发原则

- 优先完成可验证的闭环，不一次性重写全部前端。
- 每阶段先写数据库结构和接口测试，再接页面。
- 不把音频、密码、Token、生产密钥提交 GitHub。
- 不自动下载来源不明的网络歌曲。
- 不为个人项目过早增加企业级基础设施。
- 保持现有粉色 / 毛玻璃视觉体系和移动端体验。
- 每次提交前至少执行相关 lint、测试、构建和 `git diff --check`。
