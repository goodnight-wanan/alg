# 补全音乐网站功能项目计划

## 一、项目背景

原始项目：

```text
E:\Web\前端小组作业-音乐网站
```

原始仓库：

```text
https://github.com/goodnight-wanan/alg
```

原始项目是一个静态展示型半成品，页面包括：

- 首页
- 歌单分类页
- 歌单详情页
- 登录页
- 注册页

主要问题是数据写死、搜索无功能、歌单筛选无功能、登录注册无状态、播放器只能播放一首固定音乐、页面固定宽度导致移动端不可用。

---

## 二、改造方案

采用方案 B：

- Vue 3
- Vite
- Vue Router 4
- Pinia
- localStorage 模拟后端数据

新项目目录：

```text
E:\Web\new music website
```

原项目保持不变，新项目作为独立 Git 分支管理。

---

## 三、当前进度

- 主要功能已完成并持续推送到原仓库 `vue-rewrite` 分支。
- 最新提交：`2d194ef fix: remove duplicate search clear button and polish search UI`。
- 原 `master` 分支与原本地目录未改动。
- 约定：每次项目改动完成后自动提交并推送到 GitHub `vue-rewrite` 分支备份。

### 核心架构

- Vue 3 + Vite + Vue Router 4 + Pinia，localStorage 模拟后端。
- 全站统一固定顶部导航 `AppHeader.vue`（`position: fixed` + ResizeObserver 测高），移动端汉堡菜单。
- 新增 `Icon.vue` 全局 SVG 图标组件，全站 emoji / Unicode 字形已替换为 SVG，跨浏览器渲染一致。
- 新增全局提示工具 `src/utils/notice.js`（toast）。
- 通用组件：`AppHeader`、`PlayerBar`、`UserCard`、`Icon`（`SiteNav` 已删除）。

### 已完成的优化（按模块）

- 首页：外层容器 `min(1680px, 100%)` 防溢出；内容宽度改为相对父容器的百分比、封面用 `aspect-ratio`，等比例缩放；排行榜 `space-between` 居中；全站背景统一为首页背景。
- 导航：品牌 Logo、SVG 搜索、统一激活态（粉字 + 底部短横线）、登录 CTA、毛玻璃背景、移动端菜单、语义化（aria）；「主页」改为「首页」；修复 button 默认样式导致的 tab 伸缩。
- 歌单分类页：重写为 functional 体系 + chip 筛选 + 卡片网格 + 空状态，筛选同步 URL。
- 歌单详情页：重写为展示歌单信息 + 歌曲列表（去掉上传表单），支持播放全部、收藏歌单。
- 排行榜页：新增 `/rank`，榜单卡片 + 排名列表（前三金银铜），与导航、首页联动。
- 我的音乐页：`UserCard`（头像/统计/退出登录）、tab 图标 + 胶囊徽标 + 切换动画 + hover、取消收藏、整行播放、时间戳、清空记录、空状态。
- 播放器：`playAll` 点击可暂停（点当前歌曲可切换播放/暂停）、收藏按钮固定尺寸、播放/暂停图标尺寸统一、音量百分比显示。
- 用户系统：收藏歌曲/歌单/播放历史按用户 ID 分区；登录改为站内跳转（`/login?redirect=`）；退出登录跳回首页。
- 新碟页：新增 `/album`，品牌渐变 banner + 新碟卡片网格（NEW 角标、悬浮播放），数据取 `isNew` 新歌。
- 歌手页：新增 `/artist`，从歌曲聚合歌手（支持多歌手拆分），搜索 + 地区筛选 + 圆形头像卡片 + 页内歌手详情。
- 搜索页：重构为「热门搜索 / 历史搜索」引导态 + 结果态；歌曲 / 歌单 / 歌手三分类、关键词高亮、相关性排序、整行播放、清除按钮、`/` 快捷键聚焦；历史搜索持久化到 localStorage。
- 登录与提示：`openAuthWindow` 弹窗登录改为站内登录；`alert` 统一替换为 `notice` toast。
- 可访问性：轮播圆点键盘可达、卡片标题键盘操作、`role` / `aria` 语义化、`prefers-reduced-motion` 降级动画、focus 样式。
- 清理与性能：删除 `favoriteSongObjects`、`seek(seconds)`、`openAuthWindow.js`、未用图片 / 图标与 `歌单页面.css`；首页 / 登录 / 注册 CSS 通过 `preload` / `prefetch` 预取，缓解 FOUC。

---

## 四、待办事项（剩余）

### 高优先级

1. **歌曲音频仍共用同一段音频**：`src/data/musicData.js` 中所有歌曲使用 `demoAudio`，实际播放仍是同一首《只因你太美》。当前唯一 MP3 约 9.7MB。（用户决定暂不处理）
2. **剩余死链**：顶部「客户端」「VIP」、首页底部「下载客户端」仍为「演示功能」提示；登录页「忘记密码」「用户协议」无响应，QQ / 微信社交登录为演示。需实现真实页面或删除。

### 长期 / 工程化

3. **密码哈希**：`storage.js` 的 `hashPassword` 为自定义弱哈希，接入真实后端前应改为服务端校验和安全哈希（bcrypt/PBKDF2）。
4. **工程化**：增加 ESLint / Prettier。

> 已完成的清理项：死代码与未用资源已清理（`favoriteSongObjects`、`seek`、`openAuthWindow.js`、未用图片 / 图标、`歌单页面.css`）；5 张未引用图片按需求保留未删除。FOUC 已通过首页 CSS `preload`、登录 / 注册 CSS `prefetch` 缓解。

---

## 五、本地运行方式

```bash
cd "E:\Web\new music website"
npm install
npm run dev
```

构建：

```bash
npm run build
```
